services:
  api:
    build: .
    image: deepface-age-server:latest
    ports:
      - "5000:5000"
    environment:
      LOG_LEVEL: INFO
      ENABLE_CACHE: "true"
      MAX_IMAGE_BYTES: "5242880"
      RATE_LIMIT_PER_MINUTE: "60"
    restart: unless-stopped
